imageio-ffmpeg>=0.6
pillow>=12

# Optional segmentation backends used by scripts/extract_pet_states.py.
# Install only if you want to experiment with AI matting locally.
opencv-python>=4.10
rembg>=2.0
onnxruntime>=1.20

# Optional external plugin backend:
# RobustVideoMatting is intentionally not bundled into the WPF app.
# Install its own requirements in a separate Python environment when using --backend rvm.
# See F:\workspace_github\RobustVideoMatting\requirements_inference.txt.
